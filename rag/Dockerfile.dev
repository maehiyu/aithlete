FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install --no-cache-dir -r requirements.txt watchdog

EXPOSE 9200
# ホットリロード: watchdogでmain.py変更時に再起動
CMD ["watchmedo", "auto-restart", "--directory=.", "--pattern=main.py", "--recursive", "--", "python", "main.py"]
